name: sheikh_app
description: "تطبيق الشيخ د. محمد الأمين إسماعيل"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  just_audio: ^0.9.40
  audio_service: ^0.18.18
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  shimmer: ^3.0.0
  path_provider: ^2.1.4
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
